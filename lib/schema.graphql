type demo {
  id: String!
  version: String!
}
type Query {
  getDemos: [demo!]
  getDemos2: [demo!]
}
input DemoInput {
  version: String!
}
type Mutation {
  addDemo(input: DemoInput!): demo
}
